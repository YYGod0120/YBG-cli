{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CLI",
            "type": "node",
            "request": "launch",
            "args": [
                "${workspaceFolder}/src/node/cli.ts"
            ],
            "runtimeArgs": ["-r", "ts-node/register"],
            "cwd": "${workspaceRoot}",
            "program": "${workspaceFolder}/src/node/cli.ts", // 代码入口文件，绝对路径
            "internalConsoleOptions": "openOnSessionStart",
            "outFiles": ["${workspaceFolder}/dist/index.js"]// 编译后输出的位置
        }
    ]
}